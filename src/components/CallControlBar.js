import React from 'react';

const CallControlBar = () => {
    return (
        <div className="call-control-bar">
            {/* Add call control buttons here */}
            <button>Mute</button>
            <button>Hold</button>
            <button>End Call</button>
        </div>
    );
};

export default CallControlBar;
